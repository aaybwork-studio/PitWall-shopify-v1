---
context: research
phase: 03-helmets-pdp-routing
task: 0
total_tasks: 0
status: paused
last_updated: "2026-06-19T20:46:00+05:30"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Build Before Push — Compile build output `assets/pitwall-interactive.js` and `assets/main.css` locally using `npm run build` before pushing via Shopify CLI, since Shopify has no build step.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Unbuilt source code deployment | Pushing source changes without running compile script results in stale production features. | blocking | Execute `npm run build` and verify output changes before pushing. |
| Non-HTML-escaped JSON data attributes | Outputting JSON attributes directly without HTML escape breaks on variant titles with quotes. | advisory | Always use `\| json \| escape` filter in Liquid templates when passing JSON to React attributes. |

<current_state>
We are currently between phases. Phase 02.4 (Collections Redesign) and Phase 02.5 (Manifesto Section Redesign) are fully completed, built, and committed. The dynamic megamenus header and the hover cart dropdown sync with Shopify AJAX Cart are also built. The codebase mapping has been fully refreshed. We are ready to begin Phase 3 (Helmets PDP & Dynamic Routing).
</current_state>

<completed_work>
Completed Tasks:
- Refreshed all 7 codebase documentation maps to date 2026-06-19.
- Updated progress tracking in ROADMAP.md and STATE.md to reflect that all homepage interaction, car scroll, collections redesign, and manifesto image reveal phases are 100% complete.
</completed_work>

<remaining_work>
- Plan Phase 3 (Helmets PDP & Dynamic Routing) via `/gsd-plan-phase 3`.
- Implement helmet Three.js WebGL scenes, history API routing switches, and grid updates on Collections.
</remaining_work>

<decisions_made>
- Programmatic scroll state machine: Implemented a wheel state listener for homepage navigation on desktop to prevent slide-skipping from trackpad inertia.
- AJAX Cart Dropdown: Mounted a hover dropdown in the header to show line items, subtotals, and remove triggers.
- Dynamic navigation megamenus: Added Liquid megamenu grids for CATEGORIES, DRIVERS, TEAMS, and GIFTS.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — Current milestone and completion details.
2. `.planning/ROADMAP.md` — Checklist of remaining phases.
3. `.planning/codebase/ARCHITECTURE.md` — Overview of Liquid + React island layers.

## Infrastructure State
- Node.js environment with npm.
- Shopify CLI global installation required to test and push theme changes.
- Target Shopify dev store: `pitwall-c4hglmgu.myshopify.com`.

<context>
Ready to plan and execute Phase 3 (Helmets PDP) on the new device.
</context>

<next_action>
Start with: `/gsd-plan-phase 3`
</next_action>
