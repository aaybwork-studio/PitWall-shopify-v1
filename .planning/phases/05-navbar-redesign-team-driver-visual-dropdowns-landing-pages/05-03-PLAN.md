---
phase: 05-navbar-redesign-team-driver-visual-dropdowns-landing-pages
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/components/CollectionGrid.tsx
  - src/components/TeamLandingPage.tsx
  - src/components/DriverLandingPage.tsx
  - src/main.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Visiting a team collection page shows hero banner, breadcrumb, title, bio blurb, then a filter/sort bar, then a product grid"
    - "Visiting a driver collection page shows the same structure with the driver's team name linked/highlighted in the bio"
    - "Filter bar sort/category state on team/driver pages follows the same URL-param allow-list validation pattern as CollectionGrid.tsx (no raw query param trusted without Array.find validation)"
    - "The existing /collections/all page (CollectionGrid mount) renders unchanged after the refactor"
  artifacts:
    - path: "src/components/TeamLandingPage.tsx"
      provides: "Hero/breadcrumb/bio/filter-bar/grid component for team pages"
      exports: ["TeamLandingPage"]
    - path: "src/components/DriverLandingPage.tsx"
      provides: "Hero/breadcrumb/bio/filter-bar/grid component for driver pages"
      exports: ["DriverLandingPage"]
    - path: "src/main.tsx"
      provides: "Mounts TeamLandingPage at #team-landing-root and DriverLandingPage at #driver-landing-root"
      contains: "team-landing-root"
  key_links:
    - from: "src/main.tsx"
      to: "src/components/TeamLandingPage.tsx"
      via: "ReactDOM.createRoot(teamLandingRoot).render"
      pattern: "TeamLandingPage"
    - from: "src/components/TeamLandingPage.tsx"
      to: "src/components/CollectionGrid.tsx"
      via: "import { CollectionCard, useUrlSyncedFilter, SortDropdown }"
      pattern: "from ['\"]\\./CollectionGrid['\"]"
---

<objective>
Extract the reusable filter/sort state hooks and UI subcomponents from `CollectionGrid.tsx` (currently inline, lines 342-449 for state + 501-602 for UI), then build `TeamLandingPage.tsx` and `DriverLandingPage.tsx` that compose those exports with a hero/breadcrumb/bio header, reusing `CollectionCard` directly for the product grid. Wire both new components into `src/main.tsx` with new mount blocks following the existing `collection-grid-root` JSON-parse-with-fallback pattern.

Purpose: Delivers the visual landing pages (todo spec Image 3 / Image 4) without forking ~250 lines of filter logic — CONTEXT decision #5 requires reusing CollectionGrid.tsx's filter/sort logic and ASVS V5 allow-list pattern verbatim.
Output: `CollectionGrid.tsx` (extended with exported `useUrlSyncedFilter`/`useUrlSyncedSort` hooks, exported `SortDropdown` component — `CollectionCard` is already exported, no change needed there), `TeamLandingPage.tsx` (new), `DriverLandingPage.tsx` (new), `main.tsx` (additive mount blocks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/CollectionGrid.tsx
@src/main.tsx
</context>

<interfaces>
<!-- Exact existing code to extract from CollectionGrid.tsx — copy logic, do not alter behavior -->

Existing exported Product interface and CollectionCard (CollectionGrid.tsx lines 4-82) — already exported, import directly:
```typescript
export interface Product {
  title: string;
  url: string;
  price: string;
  image: string;
  specs?: string;
  category?: string;
}
export function CollectionCard({ product, style, className }: CollectionCardProps) { ... }
```

URL-param-synced category state (CollectionGrid.tsx lines 342-352, to extract into a hook):
```typescript
const [selectedCategory, setSelectedCategory] = useState<string>(() => {
  if (typeof window !== 'undefined') {
    const params = new URLSearchParams(window.location.search);
    const catParam = params.get('category');
    if (catParam) {
      const matchedCat = CATEGORIES.find(c => c.toLowerCase() === catParam.toLowerCase());
      if (matchedCat) return matchedCat;
    }
  }
  return 'All';
});
```

URL-param-synced sort state (CollectionGrid.tsx lines 354-364) — identical pattern keyed on `sort` param against a `SORTS`-like array.

popstate listener (lines 369-396), handleCategorySelect (lines 398-411), handleSortSelect (lines 413-427) — all reusable verbatim against a generic allow-list array parameter instead of the hardcoded `CATEGORIES`/`SORTS` module constants.

Sort dropdown UI markup + outside-click-close effect (lines 444-449, 537-619) — extract into an exported `SortDropdown` component taking `options: string[]`, `selected: string`, `onSelect: (s: string) => void`, `open: boolean`, `onToggle: () => void` props, preserving the exact inline styles shown in CollectionGrid.tsx.

main.tsx existing mount block to mirror (lines 213-233):
```typescript
const collectionRoot = document.getElementById('collection-grid-root');
if (collectionRoot) {
  const videoUrl = collectionRoot.getAttribute('data-video-url') || '';
  const title = collectionRoot.getAttribute('data-title') || 'Our Products';
  let products = [];
  const productsScript = document.getElementById('collection-products-data');
  if (productsScript) {
    try {
      products = JSON.parse(productsScript.textContent || '[]');
    } catch (err) {
      Logger.error('Failed to parse collection product data JSON in main.tsx', err);
    }
  }
  const root = ReactDOM.createRoot(collectionRoot);
  root.render(<React.StrictMode><CollectionGrid products={products} videoUrl={videoUrl} title={title} /></React.StrictMode>);
}
```

New data attributes available on #team-landing-root / #driver-landing-root (from Plan 02's sections/collection-team.liquid, sections/collection-driver.liquid): `data-title`, `data-hero-image`, `data-bio`, `data-logo` (team only), `data-team-name`, `data-team-link` (driver only).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract reusable filter/sort hooks and SortDropdown from CollectionGrid.tsx</name>
  <files>src/components/CollectionGrid.tsx</files>
  <read_first>
    src/components/CollectionGrid.tsx (lines 1-160, 341-620 — already read this session, full filter/sort logic and UI)
  </read_first>
  <action>
    In src/components/CollectionGrid.tsx, add two new exported hooks above the `CollectionGrid` function definition: `export function useUrlSyncedFilter(paramName: string, allowList: string[], defaultValue: string)` returning `[value, setValue, handleSelect]` — generalizes the existing `selectedCategory`/`selectedSort` state-init-from-URL + `Array.find()` allow-list-validation + popstate-listener + `handleCategorySelect`-style pushState logic (lines 342-352, 369-411) into one reusable hook parameterized by query param name and allow-list array, preserving the exact `Array.find(v => v.toLowerCase() === param.toLowerCase())` guard so no raw param value is ever trusted. Call this hook twice inside the existing `CollectionGrid` function body (once for category with `CATEGORIES`, once for sort with `SORTS`) to replace the now-duplicated inline state, verifying existing behavior is unchanged.

    Add `export function SortDropdown({ options, selected, onSelect, open, onToggle }: { options: string[]; selected: string; onSelect: (s: string) => void; open: boolean; onToggle: () => void })` extracting the sort button + dropdown markup (lines 537-619) verbatim (same inline styles, same `SORTS.map` → `options.map`), then replace the inline JSX in `CollectionGrid`'s render with `<SortDropdown options={SORTS} selected={selectedSort} onSelect={handleSortSelect} open={sortOpen} onToggle={() => setSortOpen(o => !o)} />`.

    Do not change `F1_PRODUCTS`, `CATEGORIES`, `SORTS`, `CATEGORY_DESCRIPTIONS`, `CollectionRow`, `CategoryCard`, or the outer `CollectionGrid` export signature — this is an internal refactor; the existing `/collections/all` page must render identically before and after.
  </action>
  <verify>
    <automated>grep -q 'export function useUrlSyncedFilter' "src/components/CollectionGrid.tsx" && grep -q 'export function SortDropdown' "src/components/CollectionGrid.tsx" && grep -q 'export function CollectionGrid' "src/components/CollectionGrid.tsx" && echo PASS</automated>
  </verify>
  <done>CollectionGrid.tsx exports useUrlSyncedFilter and SortDropdown in addition to its existing exports; the CollectionGrid component itself now calls useUrlSyncedFilter internally and its rendered output is unchanged (same DOM structure, same class names).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build TeamLandingPage.tsx and DriverLandingPage.tsx</name>
  <files>src/components/TeamLandingPage.tsx, src/components/DriverLandingPage.tsx</files>
  <behavior>
    - TeamLandingPage renders a hero `<img>` from `heroImage` prop, a breadcrumb (`Home / Teams / {title}`), an `<h1>` with `title` prop, a bio `<p>` with `bio` prop, then a filter bar with at minimum a Sort dropdown (reusing `SortDropdown` from CollectionGrid.tsx) and at most a Category-style tab row if `products` carry a `category` field, then a CSS grid of `CollectionCard` for each product in `products` (sorted per `useUrlSyncedFilter('sort', SORTS, 'Default')`).
    - DriverLandingPage renders the same hero/breadcrumb/title/bio/filter/grid structure, plus the bio paragraph must render `teamName` as a styled inline `<a href={teamLink}>` when both `teamName` and `teamLink` props are non-empty (todo spec: "description blurb with team name linked/highlighted").
    - Both components must render an empty-state message ("No products found.") when `products` is an empty array, matching CollectionGrid's existing empty-state pattern (lines 624-637) rather than rendering a blank grid.
  </behavior>
  <read_first>
    src/components/CollectionGrid.tsx (CollectionCard component, lines 22-82; empty-state pattern, lines 622-637; SortDropdown/useUrlSyncedFilter just added in Task 1)
  </read_first>
  <action>
    Create src/components/TeamLandingPage.tsx exporting `TeamLandingPage({ products, title, heroImage, bio, logo }: { products: Product[]; title: string; heroImage: string; bio: string; logo?: string })`. Import `Product`, `CollectionCard`, `SortDropdown`, `useUrlSyncedFilter` from `./CollectionGrid`. Define local `const SORTS = ['Default', 'Price: Low to High', 'Price: High to Low', 'Alphabetical']` (duplicate the literal array since it's not exported as a named const from CollectionGrid.tsx — only the hook/component are exported). Structure: hero section (full-width `heroImage` background or `<img>`, with `logo` overlaid if provided), breadcrumb row (`Home / Teams / {title}` as plain text links, `/` and `/collections` as hrefs), `<h1>{title}</h1>`, `<p>{bio}</p>`, sticky filter bar reusing `SortDropdown`, then a CSS grid (`display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))`) of `<CollectionCard product={p} key={p.url} />` for each product, sorted client-side using the same `getCleanPrice`-style comparator inline (re-implement the 3-line price-parse helper locally since it is not exported from CollectionGrid.tsx — do not import a non-exported helper). Apply Tailwind utility classes matching the project's brutalist token usage already seen in CollectionGrid.tsx (`var(--bg)`, `var(--fg)`, `font-mono`, `uppercase tracking-tighter`, zero border-radius).

    Create src/components/DriverLandingPage.tsx exporting `DriverLandingPage({ products, title, heroImage, bio, teamName, teamLink }: { products: Product[]; title: string; heroImage: string; bio: string; teamName?: string; teamLink?: string })` with the identical structure, except the bio rendering: split `bio` only at the point where `teamName` is referenced is NOT required — simplest correct approach is to render the bio text as-is in a `<p>`, then immediately below it (or appended via a wrapping span) a separate `<a href={teamLink} class="...">{teamName}</a>` if `teamName` is provided, labeled with a small "TEAM" prefix (e.g. `<p>{bio} <a href={teamLink}>{teamName} →</a></p>` when both are present, otherwise just `<p>{bio}</p>`). Reuse `SortDropdown`/`useUrlSyncedFilter`/`CollectionCard` exactly as in TeamLandingPage.
  </action>
  <verify>
    <automated>grep -q 'export function TeamLandingPage' "src/components/TeamLandingPage.tsx" && grep -q 'export function DriverLandingPage' "src/components/DriverLandingPage.tsx" && grep -q "from './CollectionGrid'" "src/components/TeamLandingPage.tsx" && grep -q "from './CollectionGrid'" "src/components/DriverLandingPage.tsx" && echo PASS</automated>
  </verify>
  <done>Both components exist, import CollectionCard/SortDropdown/useUrlSyncedFilter from CollectionGrid.tsx (no forked filter logic), render hero/breadcrumb/title/bio/filter-bar/grid, and DriverLandingPage links teamName when teamLink+teamName are provided.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mount TeamLandingPage and DriverLandingPage in main.tsx</name>
  <files>src/main.tsx</files>
  <read_first>
    src/main.tsx (lines 1-40, 200-240 — existing mount block pattern and imports)
  </read_first>
  <action>
    Add `import { TeamLandingPage } from './components/TeamLandingPage';` and `import { DriverLandingPage } from './components/DriverLandingPage';` near the existing `import { CollectionGrid } from './components/CollectionGrid';` line (main.tsx line 8). Immediately after the existing `collection-grid-root` mount block (main.tsx lines 213-233), add two new sibling blocks following the identical try/catch JSON-parse pattern:

    Block 1 — mounts at `#team-landing-root`: read `data-title`, `data-hero-image`, `data-bio`, `data-logo` attributes via `getAttribute`, parse `#collection-products-data` JSON with the same try/catch + `Logger.error` fallback to `[]`, then `ReactDOM.createRoot(teamLandingRoot).render(<React.StrictMode><TeamLandingPage products={products} title={title} heroImage={heroImage} bio={bio} logo={logo} /></React.StrictMode>)`.

    Block 2 — mounts at `#driver-landing-root`: same pattern, additionally reading `data-team-name` and `data-team-link` attributes, rendering `<DriverLandingPage products={products} title={title} heroImage={heroImage} bio={bio} teamName={teamName} teamLink={teamLink} />`.

    Do not modify the megamenu hover-binding block (main.tsx lines 236-298, `showDropdown`/`hideDropdown`) — it must remain byte-identical, since it is keyed by `data-dropdown`/`data-panel` string values unrelated to this plan's new mount points.
  </action>
  <verify>
    <automated>grep -q "team-landing-root" "src/main.tsx" && grep -q "driver-landing-root" "src/main.tsx" && grep -q "TeamLandingPage" "src/main.tsx" && grep -q "DriverLandingPage" "src/main.tsx" && grep -q "showDropdown" "src/main.tsx" && echo PASS</automated>
  </verify>
  <done>main.tsx mounts TeamLandingPage at #team-landing-root and DriverLandingPage at #driver-landing-root using the same JSON-parse-with-fallback pattern as the existing collection-grid-root block; the showDropdown/hideDropdown megamenu binding code is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser URL query string -> React filter state | Untrusted client-controlled input (sort/category params) |
| Liquid-rendered data attributes -> React props | Server-rendered, merchant-trusted (collection metafields), but passed through DOM attribute parsing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | `useUrlSyncedFilter` sort/category query params on team/driver pages | mitigate | Reuses the exact `Array.find()` allow-list validation from CollectionGrid.tsx — extracted hook carries the same guard, never trusts raw `params.get()` value directly in render or state |
| T-05-06 | Information Disclosure | `data-bio`/`data-team-name` attributes read via `getAttribute` then rendered into JSX text content | accept | React's JSX text interpolation HTML-escapes by default (no `dangerouslySetInnerHTML` used); Liquid's `| escape` filter (Plan 02) double-protects at the source |
</threat_model>

<verification>
1. `npm run build` (or equivalent Vite build script) compiles with zero TypeScript errors.
2. Visiting `/collections/all` still renders the existing grid identically (manual smoke check: category tabs, sort dropdown, card hover states all function as before).
3. A team/driver test page (manually assigned the new alternate template in Admin, per Plan 04) shows hero, breadcrumb, bio, sort dropdown, and product cards with no console errors.
</verification>

<success_criteria>
- CollectionGrid.tsx exports useUrlSyncedFilter + SortDropdown without changing its own rendered output.
- TeamLandingPage.tsx and DriverLandingPage.tsx exist, reuse CollectionCard/SortDropdown/useUrlSyncedFilter, never reimplement filter/sort logic.
- main.tsx mounts both new components additively; the existing megamenu and collection-grid-root mounts are untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/05-navbar-redesign-team-driver-visual-dropdowns-landing-pages/05-03-SUMMARY.md`
</output>
