---
phase: 05-navbar-redesign-team-driver-visual-dropdowns-landing-pages
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified: []
autonomous: false
requirements: []
user_setup:
  - service: shopify-admin
    why: "Theme code (Liquid/sections/templates) cannot create Shopify Collections, define metafield definitions, or assign alternate templates to a collection — these are Admin-only operations with no theme-code equivalent."
    dashboard_config:
      - task: "Create 12 Collections (one per launch-roster team/driver) with handles matching the nav block links seeded in Plan 01"
        location: "Shopify Admin -> Products -> Collections -> Create collection"
      - task: "Define collection metafields: custom.hero_image (file/image), custom.bio (multi-line text), custom.logo (file/image, teams only), custom.team_name (single line text, drivers only), custom.team_link (URL, drivers only)"
        location: "Shopify Admin -> Settings -> Custom data -> Collections -> Add definition"
      - task: "Assign collection.team.json as the Theme template for the 6 team Collections, and collection.driver.json for the 6 driver Collections"
        location: "Shopify Admin -> Products -> Collections -> {collection} -> Theme template dropdown (right sidebar)"
      - task: "Fill in each Collection's hero_image, bio, logo/team_name/team_link metafield values"
        location: "Shopify Admin -> Products -> Collections -> {collection} -> Metafields section"

must_haves:
  truths:
    - "Every team and driver entry in the navbar links to a real, working page (no 404s, no dead links)"
    - "Each team/driver page renders hero, bio, and at least one product in its grid (or a graceful empty state if no products are tagged yet)"
    - "The /collections/all All Products page is unaffected and still renders the original grid"
  artifacts:
    - path: ".planning/phases/05-navbar-redesign-team-driver-visual-dropdowns-landing-pages/05-04-SUMMARY.md"
      provides: "Record of which Collections were created, their handles, and confirmation all 12 nav links resolve to 200"
  key_links:
    - from: "sections/header-nav.liquid preset team_item/driver_item links"
      to: "Live Shopify Collections"
      via: "matching collection handles (red-bull-racing, scuderia-ferrari, mclaren-f1-team, mercedes-amg-f1-team, aston-martin-f1-team, williams-racing, max-verstappen, lewis-hamilton, charles-leclerc, lando-norris, oscar-piastri, george-russell)"
      pattern: "/collections/(red-bull-racing|scuderia-ferrari|mclaren-f1-team|mercedes-amg-f1-team|aston-martin-f1-team|williams-racing|max-verstappen|lewis-hamilton|charles-leclerc|lando-norris|oscar-piastri|george-russell)"
---

<objective>
Close the final gap between theme code and a working storefront: create the 12 Shopify Collections (6 teams + 6 drivers from the CONTEXT decision #7 launch roster), assign each the correct alternate template from Plan 02, populate hero/bio/logo metafields, and verify every navbar link from Plan 01 resolves to a real, non-404 page — per CONTEXT decision #8 ("every navbar item must link to a real, working page — no dead links").

Purpose: Theme code (sections, templates, blocks) cannot create Collections or metafield definitions — this is an Admin-only operation that has no CLI/API-free equivalent reachable from this repo's filesystem-only tooling. This plan documents the manual steps and then verifies the result end-to-end.
Output: 12 live Shopify Collections with assigned alternate templates and filled metafields; a verification pass confirming all 12 nav links return 200 and render the expected hero/bio/grid structure; `05-04-SUMMARY.md` documenting handles created and any handle mismatches found/fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@sections/header-nav.liquid
@templates/collection.team.json
@templates/collection.driver.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create 12 Collections in Shopify Admin with correct handles, templates, and metafields</name>
  <what-built>Plan 01 seeded 6 team_item and 6 driver_item theme blocks in sections/header-nav.liquid, each pointing to a /collections/{handle} URL. Plan 02 built collection-team.liquid / collection-driver.liquid sections and their alternate JSON templates, which read collection.metafields.custom.hero_image / bio / logo / team_name / team_link. None of these Collections or metafield definitions exist in the live store yet — theme code cannot create them.</what-built>
  <read_first>
    sections/header-nav.liquid (the preset block in {% schema %} lists the exact link/handle for each of the 12 entries)
  </read_first>
  <how-to-verify>
    In Shopify Admin:
    1. Go to Settings -> Custom data -> Collections -> Add definition. Create 5 metafield definitions: `hero_image` (type: File), `bio` (type: Multi-line text), `logo` (type: File), `team_name` (type: Single line text), `team_link` (type: URL). Namespace/key will resolve to `custom.hero_image` etc., matching the Liquid in sections/collection-team.liquid and sections/collection-driver.liquid.
    2. Go to Products -> Collections -> Create collection. Create 6 team Collections with these exact handles (Admin auto-generates handle from title — verify/edit the handle field to match exactly): `red-bull-racing` (title "Red Bull Racing"), `scuderia-ferrari` (title "Scuderia Ferrari"), `mclaren-f1-team` (title "McLaren F1 Team"), `mercedes-amg-f1-team` (title "Mercedes-AMG F1 Team"), `aston-martin-f1-team` (title "Aston Martin F1 Team"), `williams-racing` (title "Williams Racing"). For each, set Theme template (right sidebar dropdown) to "collection.team" (resolves to templates/collection.team.json), then fill in hero_image, bio, and logo metafield values.
    3. Create 6 driver Collections with handles: `max-verstappen`, `lewis-hamilton`, `charles-leclerc`, `lando-norris`, `oscar-piastri`, `george-russell` (titles = driver full names). Set Theme template to "collection.driver" for each, fill in hero_image, bio, team_name (the driver's current team display name), and team_link (`/collections/{team-handle}` matching the team Collection just created).
    4. For each Collection (team and driver), either add existing products to it manually or leave empty — the grid renders an empty-state message gracefully per Plan 03's behavior spec, so an empty Collection is not a blocker to shipping, but should be flagged in the SUMMARY for follow-up.
    5. Confirm scuderia-ferrari, red-bull-racing, mclaren-f1-team, and mercedes-amg-f1-team handles match what may already exist from the pre-Phase-5 hardcoded links in the old theme.liquid (these 4 collections may already exist in the store — if so, reuse them, just add the new metafields and reassign the template; do not create duplicates).
  </how-to-verify>
  <resume-signal>Reply with the 12 collection handles actually created/confirmed (or report any handle that had to differ from the seed data, e.g. due to a pre-existing collection with a different handle) so the executor can reconcile sections/header-nav.liquid's preset links if needed.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify all 12 nav links resolve and reconcile any handle mismatches</name>
  <files>sections/header-nav.liquid</files>
  <read_first>
    sections/header-nav.liquid (the preset block's team_item/driver_item link values)
  </read_first>
  <action>
    Using the handles confirmed in Task 1's resume-signal, check each of the 12 `/collections/{handle}` URLs against the live theme preview (via `npx shopify theme dev` preview URL or the published store). For any handle reported as different from the seed data in sections/header-nav.liquid's preset, edit that single `link` setting value in the preset block to match the real handle — this does not require a merchant to edit anything in the editor afterward (presets only apply on first add; if the section block instances are already live in `config/settings_data.json` after the merchant interacts with the editor, also note in the SUMMARY that the merchant must manually update that block's Link field to the corrected handle, since editing the .liquid preset alone won't retroactively change settings_data.json once blocks have been customized).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" "$(npx shopify theme info --json 2>/dev/null | grep -o 'https://[^"]*' | head -1)/collections/red-bull-racing" 2>/dev/null | grep -qE '^(200|30[0-9])' || echo "MANUAL_CHECK_REQUIRED — theme dev server not running, perform curl checks manually against the preview URL for all 12 handles"</automated>
  </verify>
  <done>All 12 /collections/{handle} URLs from sections/header-nav.liquid's preset return 200 (or the preset link is corrected to match the real handle); no navbar entry is a dead link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Merchant (Admin) -> Collection metafield values | Admin-trusted content, same trust level as existing collection.title/product data already rendered elsewhere in this theme |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Repudiation | No audit trail for who created/edited Collections in Admin | accept | Shopify Admin's own activity log covers this; out of scope for theme code |
</threat_model>

<verification>
1. All 12 navbar entries (visible via hover on TEAMS/DRIVERS panels) link to a Collection that returns HTTP 200.
2. Each team/driver page shows a hero image (or graceful placeholder if hero_image metafield is empty), the collection title, and the bio text.
3. /collections/all (All Products page) is unaffected — still uses templates/collection.json, unchanged.
</verification>

<success_criteria>
- 12 live Collections exist with correct alternate templates assigned.
- Zero dead links from the navbar (CONTEXT decision #8 satisfied).
- Any handle mismatch between Plan 01's seed data and the real store is documented and reconciled.
</success_criteria>

<output>
After completion, create `.planning/phases/05-navbar-redesign-team-driver-visual-dropdowns-landing-pages/05-04-SUMMARY.md` documenting the 12 collection handles, which metafields were populated, and any handle corrections made to sections/header-nav.liquid.
</output>
