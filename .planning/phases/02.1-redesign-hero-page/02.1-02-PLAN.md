---
phase: 02.1-redesign-hero-page
plan: "02"
type: execute
wave: 2
depends_on:
  - "02.1-01"
files_modified:
  - sections/hero-canvas.liquid
autonomous: false
requirements:
  - FND-01
  - FND-02
  - FND-03

must_haves:
  truths:
    - "Homepage displays 4 snap sections: Hero, Manifesto, The Collection, and About Us"
    - "Homepage displays a 1/3 viewport height separator video player between Manifesto and Collection"
    - "Section backgrounds toggle between light and dark modes dynamically"
    - "Cards display rounded 10px borders and borderless layout spacing"
  artifacts:
    - path: "sections/hero-canvas.liquid"
      provides: "Redesigned Liquid section layout and settings schema"
    - path: "assets/pitwall-interactive.js"
      provides: "Compiled production theme React/JS assets bundle"
---

<objective>
Re-author the homepage Liquid section, compile the production theme bundle, and perform visual verification of the snapped slide layout on the dev server.

Purpose: Restructure hero-canvas.liquid, serialize collection product data, define customizer configurations, compile final assets, and check the desktop snap scroll / mobile stack.
Output: Restructured sections/hero-canvas.liquid, compiled assets/pitwall-interactive.js, compiled assets/main.css, and verified local site.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
@~/.gemini/antigravity/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-redesign-hero-page/02.1-CONTEXT.md
@.planning/phases/02.1-redesign-hero-page/02.1-RESEARCH.md
@.planning/phases/02.1-redesign-hero-page/02.1-01-PLAN.md
@sections/hero-canvas.liquid
</context>

<tasks>
<task type="auto">
  <name>Re-author sections/hero-canvas.liquid</name>
  <files>sections/hero-canvas.liquid</files>
  <read_first>sections/hero-canvas.liquid, sections/page-about.liquid</read_first>
  <action>Re-author sections/hero-canvas.liquid to implement the 4-section snapping layout structure matching Image 2:
- Apply `.homepage-snap` scroll container classes.
- Section 1 (Hero): Snap target, dark background, hosts cinematic hero video.
- Ticker: Marquee text bar dividing Hero and Manifesto.
- Section 2 (Manifesto): Snap target, light background (var(--bg)), 2-column flex/grid. Left column: brand image. Right column: manifesto title card and place holder text card.
- Separator Video: Full-bleed 1/3 viewport height container mount point `#separator-video-root`.
- Section 3 (The Collection): Snap target, inverted dark background (section-dark class). Mount point `#collection-grid-root` and output `#collection-products-data` script JSON containing title, URL, price, image, and specs for products.
- Section 4 (About Us): Snap target, light background, 2-column flex/grid. Left: "About us" title card + Photo card. Right: Photo card + Quote card. (On mobile, hide both photo cards and show only Title + Quote).
- Footer: Snap target at the bottom.
- Schema: Expose settings for `collection` (type: collection), and three fields `separator_video_1`, `separator_video_2`, `separator_video_3` (type: text) for video paths.</action>
  <verify>grep -q "collection-products-data" sections/hero-canvas.liquid</verify>
  <acceptance_criteria>
    - sections/hero-canvas.liquid contains "separator-video-root"
    - sections/hero-canvas.liquid contains "collection-grid-root"
    - sections/hero-canvas.liquid contains "collection-products-data"
    - sections/hero-canvas.liquid has {% schema %} settings for collection and videos
  </acceptance_criteria>
  <done>hero-canvas.liquid layout re-authored successfully</done>
</task>

<task type="auto">
  <name>Compile production theme assets bundle</name>
  <files>assets/pitwall-interactive.js, assets/main.css</files>
  <read_first>package.json, vite.config.ts, tailwind.config.js</read_first>
  <action>Run the Vite compiler to bundle all typescript React files and Tailwind styles:
- Run `npm run build` command in root folder.
- Confirm compilation succeeds without TS errors and outputs files to assets/ folder.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - Command npm run build exits 0
    - assets/pitwall-interactive.js size is greater than 10000 bytes
  </acceptance_criteria>
  <done>Production assets bundle compiled successfully</done>
</task>

<task type="auto">
  <name>Start local dev server</name>
  <action>Run shopify dev server in the background:
- Execute `npm run shopify:dev` or `npx shopify theme dev --store=16qgme-tu.myshopify.com` in background.
- Wait for the server to boot up and print the local dev store access URL.</action>
  <verify>fetch http://127.0.0.1:9292 returns 200 or 302</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hero Page Redesign - server running at http://127.0.0.1:9292</what-built>
  <how-to-verify>Visit the homepage on http://127.0.0.1:9292. Verify:
1. Snapping: Scroll snaps desktop sections (Hero, Manifesto, Collection, About Us) to viewport boundaries (100vh height locked).
2. Video Player: 1/3 viewport height player plays muted, autoplaying video playlist correctly.
3. Card Styles: Cards have white background, rounded corner radius 10px, and borderless float spacing.
4. Dynamic Collection Grid: Cards show dynamic product information cycling every 5-7 seconds, and hover displays detail slide-up.
5. Mobile Responsiveness: Stacks all cards and sections vertically in a single column below 767px, and About Us section hides both photos.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<verification>
- [ ] npm run build compiles successfully
- [ ] Local Shopify dev server serves the redesigned index page
</verification>

<success_criteria>
- Scroll-snapping desktop sections functioning
- Responsive mobile stacking and video aspect ratio scaling validated
- Rounded white cards spacing verified
- Product card interval switching verified
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-redesign-hero-page/02.1-02-SUMMARY.md`
</output>
