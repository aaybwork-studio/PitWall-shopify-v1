# Phase 02.1: Redesign Hero Page - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-06
**Phase:** 02.1-redesign-hero-page
**Areas discussed:** Section Background Colors, Dynamic Product Card Rotation, Manifesto Video Player, Card Styling & Spacing, Layout Sizing & Responsive Behavior

---

## Section Background Colors

| Option | Description | Selected |
|--------|-------------|----------|
| Alternating Dark and Light sections | (e.g., Hero: Dark, Manifesto: Light, Collection: Dark, About Us: Light) to maximize brutalist visual rhythm. | |
| Pure Light theme with subtle shifts | Using Pitwall's secondary surface tokens (e.g., Hero: `#EDEBE5`, Manifesto: `#F5F4F0`, Collection: `#EDEBE5`, About Us: `#E5E3DD`). | |
| Mode-Adaptive Mapping | Bind sections to CSS custom properties so they alternate and toggle automatically with dark/light mode to suit the brand. | ✓ |

**User's choice:** Mode-Adaptive Mapping (Hero stays dark, others toggle/alternate).
**Notes:** Sourced directly from user's custom input.

---

## Dynamic Product Card Rotation

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-rotating interval with a smooth fade | (e.g., every 5-7 seconds, a different product fades in, keeping the slide-up details overlay on hover). | ✓ |
| Hover-triggered rotation | Hovering over a card cycles through different angles or products, while idle displays a static product. | |
| You decide | Builder autocalibrates a performance-optimized fade-loop. | |

**User's choice:** Auto-rotating interval with a smooth fade.
**Notes:** Standard option chosen.

---

## Dynamic Product Sourcing

| Option | Description | Selected |
|--------|-------------|----------|
| Shopify Collection loop | (e.g., pulling from the 'all' collection, with a robust hardcoded static fallback if the collection is empty). | ✓ |
| Directly configured | A hardcoded set of products directly configured in the section settings schema. | |
| You decide | Builder implements collection loop with static fallback. | |

**User's choice:** Shopify Collection loop with robust fallback.
**Notes:** Reuses established fallback patterns.

---

## Separator Video Player

| Option | Description | Selected |
|--------|-------------|----------|
| Muted, autoplaying, looping playlist | Similar to the hero video, using a wide cinematic aspect ratio like 21:9 or 2.39:1. | ✓ |
| Single high-quality video with custom hover/overlay controls | Custom overlay mute/unmute and play/pause buttons. | |
| You decide | Builder autocalibrates a silent cinematic loop. | |

**User's choice:** Muted, autoplaying, looping playlist.
**Notes:** Placed between Manifesto and Collection sections as a separator element.

---

## Video Sourcing

| Option | Description | Selected |
|--------|-------------|----------|
| Configurable via Shopify Customizer | Merchants can input up to 3 custom video URLs, with pre-configured default video paths. | ✓ |
| Hardcoded video files | Pre-bundled inside the theme assets folder for zero configuration and maximum loading speed. | |
| You decide | Builder implements customizable settings with defaults. | |

**User's choice:** Configurable via Shopify Customizer settings with defaults.
**Notes:** Standard option chosen.

---

## Card Separation

| Option | Description | Selected |
|--------|-------------|----------|
| Solid brutalist borders | E.g., `1px solid var(--border)` or `1px solid var(--fg)` enclosing each card to match Pitwall's technical grid aesthetic. | |
| Borderless spacing | Cards float with clean gaps between them, relying on their white background for visual separation. Corner radius 10px. | ✓ |
| You decide | Builder autocalibrates a clean bordered layout. | |

**User's choice:** Borderless spacing (cards float with clean gaps). Corner radius 10px.
**Notes:** User added an explicit constraint for 10px corner radius on these homepage cards.

---

## Card Padding

| Option | Description | Selected |
|--------|-------------|----------|
| Uniform technical padding | E.g., `24px` on desktop, scaling down to `16px` on mobile to balance text labels and product details cleanly. | ✓ |
| Spacious editorial padding | E.g., `40px` on desktop, scaling down to `24px` on mobile to give text and photos a luxury feel. | |
| You decide | Builder autocalibrates padding for layout readability. | |

**User's choice:** Uniform technical padding.
**Notes:** Standard option chosen.

---

## Height Sizing

| Option | Description | Selected |
|--------|-------------|----------|
| Adaptable Height | `min-height: 100vh` on desktop. Each section starts full-screen but can grow if content overflows. | |
| Strict Height | `height: 100vh` on desktop. Lock each section strictly to the viewport height, scaling images or hiding overflow. | ✓ |
| You decide | Builder implements `min-height: 100vh` to avoid layout clipping. | |

**User's choice:** Strict Height (`height: 100vh` on desktop).
**Notes:** Creates a presentation slide-by-slide snapping layout.

---

## Mobile Sizing

| Option | Description | Selected |
|--------|-------------|----------|
| Stacked natural scrolling | Disable the strict `100vh` height on mobile. Let elements stack vertically and scroll naturally to avoid text truncation and tiny photos. | ✓ |
| Keep full-screen layout | Retain the strict `100vh` snaps on mobile, scaling all cards and images down to fit the small viewport. | |
| You decide | Builder implements natural stacked scrolling on mobile. | |

**User's choice:** Stacked natural scrolling.
**Notes:** Disables strict viewport height on viewports below 767px.

---

## Manifesto Video Mobile Scaling

| Option | Description | Selected |
|--------|-------------|----------|
| Preserve aspect ratio | Scale the video container dynamically to maintain its wide cinematic aspect ratio (e.g., 21:9), causing the height to adjust based on screen width. | ✓ |
| Fixed screen height | Lock the player to exactly 1/3 of the mobile screen height, which will crop the video's sides. | |
| You decide | Builder preserves aspect ratio on mobile to prevent cropping. | |

**User's choice:** Preserve aspect ratio.
**Notes:** Standard option chosen.

---

## About Us Mobile Stacking

| Option | Description | Selected |
|--------|-------------|----------|
| Stack in reading order | Show the "About Us" title card first, followed by the left column's photo, the right column's photo, and then the Quote card at the bottom. | |
| Simplified stack | Show only the "About Us" title card and the Quote card, hiding the two photo cards on mobile to keep the layout concise. | ✓ |
| You decide | Builder stacks all 4 cards in reading order. | |

**User's choice:** Simplified stack (hide the two photo cards on mobile).
**Notes:** Simplifies and shortens the vertical layout for mobile users.

---

## the agent's Discretion
- Sourcing high-quality minimalist photos for placeholders.
- Layout details and exact margins between cards.
