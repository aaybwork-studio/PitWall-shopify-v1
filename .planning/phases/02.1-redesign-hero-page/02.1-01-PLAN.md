---
phase: 02.1-redesign-hero-page
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CollectionGrid.tsx
  - src/index.css
  - src/main.tsx
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-03

must_haves:
  truths:
    - "CollectionGrid rotates through product cards at a defined interval"
    - "CollectionGrid rotation pauses when hovering on any card"
  artifacts:
    - path: "src/components/CollectionGrid.tsx"
      provides: "Dynamic rotating product card grid"
    - path: "src/main.tsx"
      provides: "Mounting points for CollectionGrid and separator VideoBackground"
---

<objective>
Implement the core stylesheet updates and React components required for the hero page redesign.

Purpose: Set up CSS snap-scroll styles, responsive breakpoints, white card aesthetics, and create/mount the dynamic CollectionGrid component.
Output: New CollectionGrid.tsx component, updated index.css styling rules, and updated main.tsx bootstrap mounts.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-redesign-hero-page/02.1-CONTEXT.md
@.planning/phases/02.1-redesign-hero-page/02.1-RESEARCH.md
@src/index.css
@src/main.tsx
</context>

<tasks>
<task type="auto">
  <name>Create CollectionGrid component</name>
  <files>src/components/CollectionGrid.tsx</files>
  <read_first>src/components/VideoBackground.tsx, src/components/Typography.tsx</read_first>
  <action>Create new React component src/components/CollectionGrid.tsx. The component must:
- Accept products prop: array of Product objects containing title, url, price, image, specs.
- Expose a step index state S. On a useEffect interval of 6000ms, increment S (only when not hovered).
- Render a grid of 6 blocks (3 columns):
  - Card 0 (Col 1, Row 1): Product slot 0 (index S*5 % N).
  - Card 1 (Col 1, Row 2): Product slot 1 (index (S*5 + 1) % N).
  - Card 2 (Col 2, Row 1): Product slot 2 (index (S*5 + 2) % N).
  - Card 3 (Col 2, Row 2): Product slot 3 (index (S*5 + 3) % N).
  - Card 4 (Col 3, Row 1-2 tall): Product slot 4 (index (S*5 + 4) % N).
  - Card 5 (Col 3, bottom): Title card "THE COLLECTION" (styled in Syne display, static, non-rotating).
- Use local hovering states to halt the rotation interval.
- Cards must use a white background, no outer border, and display details overlay (title, specs, price) sliding up on hover.
- Apply 10px corner radius to cards.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - src/components/CollectionGrid.tsx contains "export function CollectionGrid"
    - src/components/CollectionGrid.tsx contains "setInterval" and "clearInterval"
    - src/components/CollectionGrid.tsx has hover state tracking
  </acceptance_criteria>
  <done>CollectionGrid component compiled successfully</done>
</task>

<task type="auto">
  <name>Update global stylesheet styles</name>
  <files>src/index.css</files>
  <read_first>src/index.css</read_first>
  <action>Add global and layout styles to src/index.css:
- CSS scroll snap classes for min-width 768px: `.homepage-snap` on html element, and `.snap-section` on section elements.
- Separator video class `.separator-video-container` (height: 33.333vh on desktop, aspect-ratio: 21/9 on mobile).
- Add mode-adaptive background classes `.section-dark` and `.section-light` with css variables overrides.
- Add card layout helper classes: `.pw-white-card` (background-color: #FFFFFF, color: #0C0C0C, border-radius: 10px, padding: 24px desktop / 16px mobile, borderless margins).
- Override global zero border-radius constraint selectively for cards inside `.homepage-snap` context to ensure they render at 10px radius.</action>
  <verify>grep -q "homepage-snap" src/index.css</verify>
  <acceptance_criteria>
    - src/index.css contains "scroll-snap-type: y mandatory"
    - src/index.css contains "border-radius: 10px"
    - src/index.css contains "aspect-ratio: 21 / 9"
  </acceptance_criteria>
  <done>index.css updated with styling rules</done>
</task>

<task type="auto">
  <name>Mount new islands in main.tsx</name>
  <files>src/main.tsx</files>
  <read_first>src/main.tsx, src/components/CollectionGrid.tsx</read_first>
  <action>Update src/main.tsx:
- Import `CollectionGrid` from `./components/CollectionGrid`.
- Add mount logic for `#separator-video-root`. Parse the `data-video-playlist` attribute as JSON, and render `<VideoBackground playlist={playlist} />`.
- Add mount logic for `#collection-grid-root`. Parse the script tag `#collection-products-data` content (JSON string) and pass it as `products` to `<CollectionGrid />`. If empty or absent, supply a static default fallback array of at least 6 mock products.
- Ensure navbar scroll listeners dynamically target `.hero-section` or fallback cleanly if sections trigger snapping.</action>
  <verify>tsc --noEmit passes</verify>
  <acceptance_criteria>
    - src/main.tsx imports "CollectionGrid"
    - src/main.tsx contains "separator-video-root"
    - src/main.tsx contains "collection-grid-root"
  </acceptance_criteria>
  <done>main.tsx bootstraps the new interactive mounts</done>
</task>
</tasks>

<verification>
- [ ] tsc --noEmit type checks pass
- [ ] git status contains modified files in src/
</verification>

<success_criteria>
- Spacing and snapping CSS selectors compiled
- CollectionGrid React component created and mounted
- Fallback data defined for empty grid mounts
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-redesign-hero-page/02.1-01-SUMMARY.md`
</output>
