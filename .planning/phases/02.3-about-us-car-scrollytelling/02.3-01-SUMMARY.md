---
phase: 02.3-about-us-car-scrollytelling
plan: "01"
subsystem: scroll-engine
tags: [scroll, wheel-handler, group3, props-interface, typescript]
dependency_graph:
  requires: []
  provides: [group3Ref, activeIndex3, group3ScrollLeft, HomepageScrollytellingProps-12-fields]
  affects: [HomepageScrollytelling.tsx]
tech_stack:
  added: []
  patterns: [contiguous-scroll-state-machine, zone-lock, snap-assist]
key_files:
  created: []
  modified:
    - src/components/HomepageScrollytelling.tsx
decisions:
  - "Used void [] array expression to satisfy noUnusedLocals for Wave 2 forward-declared variables"
  - "group3Ref null-guard (if !g3 return) silently skips handler until Plan 03 mounts Group 3 DOM — intentional"
metrics:
  duration_minutes: 15
  completed_date: "2026-06-07"
---

# Phase 02.3 Plan 01: Fix Group 2 Exit + Group 3 Scroll Zone Summary

**One-liner:** Fixed Group 2 scroll exit to snap to Group 3 (not footer), added G3 wheel-lock zone handler, declared group3Ref/activeIndex3/group3ScrollLeft, and expanded HomepageScrollytellingProps with 12 About Us fields.

## What Was Done

### Task 1: Fix Group 2 exit + replace stale snap condition + add g3 null-check and zone declarations

**Commit:** 6f034e5

**Changes made:**

1. **Expanded `HomepageScrollytellingProps` interface** — added 12 optional props (`aboutHeading`, `foundingStory`, `ctaLabel`, `ctaUrl`, `stat1Value/Label` ×3, `brandParagraph`, `exploreCta`) with defaults in function destructuring.

2. **Added Group 3 refs and state** — `group3Ref`, `activeIndex3`, `group3ScrollLeft` declared alongside existing Group 1/2 counterparts.

3. **Extended wheel handler null-check** — `if (!g1 || !g2 || !g3) return` guard added; `g3Top` and `isAtG3` zone variable declared.

4. **Fixed Group 2 exit** — replaced `return; // Allow natural scroll to footer` with `container.scrollTo({ top: g3Top, behavior: 'smooth' })`.

5. **Added Group 3 wheel-lock zone** — `if (isAtG3)` block handles horizontal scroll, reverse-snap to G2, and passthrough at G3 end.

6. **Replaced stale footer snap with three proximity conditions:**
   - Scrolling down into G3 (within 60vh)
   - Scrolling up from vertical section back to G3
   - Scrolling up between G3 and G2 snaps to G2

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] TypeScript noUnusedLocals on Wave 2 forward-declared variables**
- **Found during:** Task 1 — tsc verification
- **Issue:** 14 variables (`aboutHeading`, `ctaLabel`, etc., plus `activeIndex3`, `group3ScrollLeft`) fail `noUnusedLocals` because their JSX consumers arrive in Plans 02/03
- **Fix:** Added `void [...]` array expression referencing all forward-declared variables before mobile return; array form avoids TS2695 comma operator error
- **Files modified:** src/components/HomepageScrollytelling.tsx
- **Commit:** 6f034e5

## Known Stubs

None. This plan is logic/state only — no JSX rendered.

## Threat Flags

None. All changes are within-component DOM mutations on same-origin scroll container. Guard `if (!g3) return` satisfies T-02.3-01-03 mitigation.

## Self-Check: PASSED

- [x] `src/components/HomepageScrollytelling.tsx` modified
- [x] Commit 6f034e5 exists
- [x] `npx tsc --noEmit` exits 0
- [x] group3Ref: 3 matches
- [x] g3Top + behavior + smooth: 3 matches
- [x] Stale "let it scroll naturally to footer": 0 matches
- [x] Stale "Scrolling up from footer": 0 matches
- [x] aboutHeading: 3 matches
- [x] group3ScrollLeft: 2 matches
- [x] isAtG3: 2 matches
