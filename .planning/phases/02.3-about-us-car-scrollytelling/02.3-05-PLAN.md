---
phase: 02.3-about-us-car-scrollytelling
plan: 05
type: execute
wave: 3
depends_on:
  - "02.3-02"
  - "02.3-03"
files_modified:
  - src/index.css
autonomous: false
requirements:
  - Mobile Group 3 layout verified (D-15)
  - .car-silhouette mobile override confirmed working
  - Vertical stats section height verified on short screens

must_haves:
  truths:
    - "Below 768px, Group 3 horizontal panels stack vertically with no horizontal scroll"
    - "Below 768px, the car renders as a static visible image — no translateX animation"
    - "Vertical stats section does not hard-clip on screens shorter than 700px"
    - "Stats section allows content to scroll beyond min-height if needed"
  artifacts:
    - path: "src/index.css"
      provides: "Any additional mobile overrides needed after visual verification"
      contains: ".car-silhouette"
  key_links:
    - from: ".horizontal-scroll-group mobile rule"
      to: "Group 3 panels stacking vertically"
      via: "flex-direction: column from existing @media max-width 767px"
      pattern: "flex-direction.*column"
    - from: ".car-silhouette mobile override"
      to: "static car rendering"
      via: "position: static from @media max-width 767px"
      pattern: "car-silhouette.*position.*static"

user_setup: []
---

<objective>
Verify and complete mobile responsive behavior for Group 3. The existing @media (max-width: 767px) rules in src/index.css already collapse .horizontal-scroll-group and .horizontal-slide to vertical layout — Group 3 inherits this automatically. This plan's job is to verify the inheritance works, identify any gaps, and add targeted fixes if needed.

Purpose: D-15 mandates mobile collapse with static car. Plan 02 already added the .car-silhouette mobile override (position: static, will-change: auto). This plan checks real rendering and adds corrections.

Output: Confirmed mobile behavior with any missing CSS overrides in src/index.css. Human checkpoint verifies the mobile layout looks correct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02.3-about-us-car-scrollytelling/02.3-CONTEXT.md
@.planning/phases/02.3-about-us-car-scrollytelling/02.3-RESEARCH.md
@.planning/phases/02.3-about-us-car-scrollytelling/02.3-02-SUMMARY.md
@.planning/phases/02.3-about-us-car-scrollytelling/02.3-03-SUMMARY.md

<interfaces>
<!-- Existing mobile CSS rules that Group 3 inherits automatically -->

From src/index.css @media (max-width: 767px):
```css
.homepage-scroll-container {
  position: static;
  overflow: auto;
  height: auto;
}
.horizontal-scroll-group {
  flex-direction: column;
  width: 100%;
  height: auto;
  overflow: visible;
}
.horizontal-slide {
  width: 100%;
  min-width: 100%;
  height: auto;
  min-height: 100vh;
}
/* .car-silhouette mobile override — added in Plan 02: */
.car-silhouette {
  position: static;
  width: 80vw;
  max-width: 480px;
  will-change: auto;
}
```

Mobile JSX branch — Group 3 sections (added in Plan 03):
- About Us section: static F1CarSilhouette with width: 80vw, maxWidth: 480px
- Stats section: stacked stat columns with border-top dividers
- No translateX applied in mobile branch
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the project and audit mobile CSS for Group 3 gaps</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css — read the full @media (max-width: 767px) block and confirm: (1) .car-silhouette override is present (added in Plan 02), (2) no conflicting rules would make .horizontal-slide panels overflow horizontally on mobile.
    - src/components/HomepageScrollytelling.tsx — read the mobile return block to check that the Group 3 mobile sections use inline styles compatible with the CSS cascade (no conflicting position/width declarations).
  </read_first>
  <action>
    Run npm run build to compile the full project. Then audit src/index.css for any mobile gaps specific to Group 3:

    CHECK 1: Confirm .car-silhouette override is inside @media (max-width: 767px) block.
    If missing: add it to the existing media query block:
      .car-silhouette { position: static; width: 80vw; max-width: 480px; will-change: auto; }

    CHECK 2: The vertical stats section uses minHeight: '100vh' as an inline style. On screens shorter than 600px (e.g., iPhone SE), 3 stat columns stacked may overflow. Verify the stats section div has no overflow:hidden that would clip content.
    If the vertical stats section parent has overflow: hidden that clips content on short screens: add to src/index.css:
      .about-stats-section { overflow: visible !important; }
    But only add this rule if the overflow clip is actually observed — do not add preemptively.

    CHECK 3: Group 3 desktop panels (Panel 1, 2, 3) have .car-silhouette with position: absolute and bottom: 20%. On mobile, position: static means the car appears in normal document flow. Verify the mobile JSX branch (which renders F1CarSilhouette without .car-silhouette class) is what renders on mobile — not the desktop panels. The `if (isMobile) return (...)` guard ensures this.

    CHECK 4: Panel 2's inline SVG telemetry (position: absolute, inset: 0) — on mobile this panel only renders in the desktop branch. Mobile branch shows the About Us stacked section, not the telemetry panel. Confirm no bleed-through.

    If all checks pass with no gaps: no CSS changes needed. Proceed to the human checkpoint.
    If gaps found: make the minimal targeted fix in src/index.css inside the existing @media block.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 (no errors)
    - grep -n "car-silhouette" src/index.css returns at least 2 matches (base rule + mobile override)
    - The mobile override is inside the @media (max-width: 767px) block — confirmed by: the car-silhouette line number falls within the media query line range
    - No new raw hex values added to src/index.css
  </acceptance_criteria>
  <done>Build succeeds. .car-silhouette mobile override confirmed present. Any gap fixes applied. No new hex tokens introduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mobile Group 3 layout including static car rendering, stacked About Us panel, stacked stats columns, and the full vertical flow from Group 2 end to Footer on mobile viewport.</what-built>
  <how-to-verify>
    1. Open browser DevTools and set viewport to 375px width (iPhone SE) or use a physical mobile device.
    2. Load the Shopify theme preview or run local dev server.
    3. Scroll through the full page on mobile — verify the scroll is natural vertical, no horizontal scroll anywhere.
    4. Confirm the F1CarSilhouette appears as a static pale silhouette (no movement as you scroll).
    5. Confirm the About Us section shows: "// ABOUT US" label, static car, founding story text, and "OUR STORY" CTA link.
    6. Confirm the stats section shows 3 stacked rows: "ZERO / COMPROMISE", "ONE / PURSUIT", "INFINITE / PRECISION" (or whatever schema defaults rendered).
    7. Confirm the brand paragraph appears below the stats.
    8. Confirm the "EXPLORE OUR STORY" CTA appears and links correctly.
    9. Check at 768px+ that the horizontal scroll behavior for Group 3 is intact (optional — will be fully verified in Plan 06).
  </how-to-verify>
  <resume-signal>Type "approved" if mobile layout is correct, or describe any specific issues (e.g., "car overflows panel", "stats clip on short screen")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile CSS media query | CSS-only boundary — no JavaScript involved in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.3-05-01 | Denial of Service | will-change: transform on mobile | mitigate | Plan 02 already adds will-change: auto override in mobile media query — GPU layer not promoted on mobile |
| T-02.3-05-02 | Tampering | mobile overflow visible on stats section | accept | Overflow-visible is a layout property — no security surface; content is static text |
</threat_model>

<verification>
- npm run build passes
- Mobile viewport (375px): no horizontal scroll anywhere on page
- F1CarSilhouette visible as static image in About Us mobile section
- Stats columns stack vertically and are fully readable
- All CTAs are tappable links
</verification>

<success_criteria>
Build succeeds. Mobile Group 3 renders as stacked vertical sections with static car. Human checkpoint approves the visual layout. No CSS token violations.
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-about-us-car-scrollytelling/02.3-05-SUMMARY.md`
</output>
