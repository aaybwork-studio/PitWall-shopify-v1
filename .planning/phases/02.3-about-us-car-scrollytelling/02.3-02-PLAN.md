---
phase: 02.3-about-us-car-scrollytelling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HomepageScrollytelling.tsx
  - src/index.css
autonomous: true
requirements:
  - F1CarSilhouette inline SVG JSX component defined (D-01, D-12)
  - .car-silhouette CSS class added to src/index.css (no new tokens — D-13)
  - Car animation formula implemented (D-03)

must_haves:
  truths:
    - "F1CarSilhouette is a named function component at file scope in HomepageScrollytelling.tsx"
    - "Car renders as a recognizable F1 side-profile silhouette (chassis, wings, 2 wheels, cockpit, halo) in WM.text color"
    - ".car-silhouette CSS class positions car absolutely at bottom-20% of its parent slide"
    - "carX, rawProgress, and easedProgress are computed inline in the desktop JSX render block from group3ScrollLeft"
    - "Car animation uses ease-out (1 - (1-progress)^1.5) not linear"
  artifacts:
    - path: "src/components/HomepageScrollytelling.tsx"
      provides: "F1CarSilhouette component + carX formula"
      contains: "F1CarSilhouette"
    - path: "src/index.css"
      provides: ".car-silhouette positioning rule + mobile override"
      contains: ".car-silhouette"
  key_links:
    - from: "group3ScrollLeft state"
      to: "carX vw value"
      via: "rawProgress / easedProgress formula"
      pattern: "group3ScrollLeft.*group3MaxScroll"
    - from: ".car-silhouette class"
      to: "F1CarSilhouette SVG"
      via: "wrapper div with transform: translateX(${carX}vw)"
      pattern: "car-silhouette.*translateX"
---

<objective>
Create the F1CarSilhouette inline SVG JSX component and add the .car-silhouette CSS class. Implement the car animation formula (carX from group3ScrollLeft) in the desktop render. This plan can run in parallel with Plan 01 because it touches different parts of the same file (new component above the main component, new CSS class, and a new computed value in the JSX render — no overlap with Plan 01's wheel handler changes).

Purpose: The car is the visual centerpiece of Group 3. Getting the component right here means Plans 03 and 04 can reference F1CarSilhouette and carX directly without re-implementing the formula.

Output: F1CarSilhouette function component, carX formula ready to bind in JSX, .car-silhouette CSS class in src/index.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02.3-about-us-car-scrollytelling/02.3-CONTEXT.md
@.planning/phases/02.3-about-us-car-scrollytelling/02.3-RESEARCH.md
@.planning/phases/02.3-about-us-car-scrollytelling/02.3-PATTERNS.md

<interfaces>
<!-- Pattern analogs from the codebase the executor should mirror -->

HeroTicker (lines 40-62) — the component definition pattern to mirror:
```typescript
interface TickerProps { offset: number; itemRef: React.RefObject<any>; }
function HeroTicker({ offset, itemRef }: TickerProps) {
  // ... returns JSX
}
```

WM object (lines 8-13) — already file-scope, reference directly in F1CarSilhouette:
```typescript
const WM = {
  bg:   '#0F0C09',
  bg2:  '#140F0B',
  gold: '#E8B93B',
  text: '#EDE8E0',
};
```

Ticker offset apply pattern (line 51) — same transform pattern for the car:
```typescript
style={{ transform: `translate3d(${offset}px, 0, 0)` }}
// Car equivalent:
style={{ transform: `translateX(${carX}vw)` }}
```

group3ScrollLeft state (added in Plan 01, lines ~165-166):
```typescript
const [group3ScrollLeft, setGroup3ScrollLeft] = useState(0);
```

src/index.css — .horizontal-slide is the parent context for .car-silhouette:
```css
.horizontal-slide {
  width: 100vw;
  min-width: 100vw;
  height: 100vh;
  flex-shrink: 0;
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add F1CarSilhouette inline SVG component to HomepageScrollytelling.tsx</name>
  <files>src/components/HomepageScrollytelling.tsx</files>
  <read_first>
    - src/components/HomepageScrollytelling.tsx — read to find the exact insertion point (after HeroTicker ends at approximately line 62, before DesignedTitleCard at approximately line 65). Insert F1CarSilhouette between these two.
    - .planning/phases/02.3-about-us-car-scrollytelling/02.3-PATTERNS.md — Pattern 7 contains the full SVG component code including all shape elements.
  </read_first>
  <action>
    Insert the F1CarSilhouette function component into src/components/HomepageScrollytelling.tsx between the HeroTicker closing brace and the DesignedTitleCard definition. Add a section comment "─── F1 Car Silhouette ───" to match the existing file structure.

    The component definition:
    ```
    function F1CarSilhouette({ color = '#EDE8E0' }: { color?: string }) {
      return (
        <svg
          viewBox="0 0 800 200"
          xmlns="http://www.w3.org/2000/svg"
          style={{ width: '100%', height: '100%' }}
          fill={color}
          aria-hidden="true"
        >
          {/* Main chassis body */}
          <rect x="80" y="110" width="580" height="45" rx="8" />
          {/* Cockpit bump */}
          <rect x="310" y="80" width="160" height="35" rx="12" />
          {/* Halo (thin arc above cockpit) */}
          <path d="M320 80 Q390 55 470 80" stroke={color} strokeWidth="6" fill="none" />
          {/* Sidepods */}
          <rect x="240" y="108" width="120" height="25" rx="4" />
          <rect x="440" y="108" width="120" height="25" rx="4" />
          {/* Front wing */}
          <rect x="30" y="148" width="100" height="8" rx="2" />
          <rect x="30" y="140" width="6" height="16" rx="1" />
          <rect x="124" y="140" width="6" height="16" rx="1" />
          {/* Front wing strut */}
          <rect x="70" y="130" width="30" height="20" rx="2" />
          {/* Rear wing */}
          <rect x="640" y="85" width="110" height="8" rx="2" />
          <rect x="680" y="93" width="6" height="25" rx="1" />
          <rect x="704" y="93" width="6" height="25" rx="1" />
          {/* Front wheel */}
          <ellipse cx="150" cy="168" rx="32" ry="32" />
          <ellipse cx="150" cy="168" rx="14" ry="14" fill={WM.bg} />
          {/* Rear wheel */}
          <ellipse cx="620" cy="168" rx="36" ry="36" />
          <ellipse cx="620" cy="168" rx="16" ry="16" fill={WM.bg} />
          {/* Nose cone */}
          <path d="M80 125 L20 148 L80 155 Z" />
        </svg>
      );
    }
    ```

    The wheel hub ellipses use `fill={WM.bg}` (not `fill={color}`) to create a cutout effect showing the wheel rim. WM is already file-scope so this reference is valid inside the component.

    Do NOT add any TypeScript types beyond the inline prop type `{ color?: string }`. Do not import any new libraries.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -n "F1CarSilhouette" src/components/HomepageScrollytelling.tsx` returns at least 1 match (the function definition line)
    - `grep -n "viewBox.*0 0 800 200" src/components/HomepageScrollytelling.tsx` returns 1 match
    - `grep -n "aria-hidden" src/components/HomepageScrollytelling.tsx` returns at least 1 match (accessibility attribute present)
    - The component contains at least 12 shape elements (rect, ellipse, path — count from the SVG block)
  </acceptance_criteria>
  <done>F1CarSilhouette is a named function component at file scope. The SVG has viewBox="0 0 800 200", aria-hidden="true", and contains all required F1 shapes: chassis body, cockpit, halo, sidepods, front/rear wings, front/rear wheels with rim cutouts, and nose cone. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add .car-silhouette CSS class to src/index.css + add carX formula in desktop JSX</name>
  <files>src/index.css, src/components/HomepageScrollytelling.tsx</files>
  <read_first>
    - src/index.css — read to find: (1) the .horizontal-slide block to understand the positioning context, (2) the @media (max-width: 767px) block to find where to add the mobile .car-silhouette override, (3) the exact line after .horizontal-slide.grid-slide-wide to insert the new rule.
    - src/components/HomepageScrollytelling.tsx — read the desktop return block (starting around line 511) to find the correct location to insert the carX formula. It should be computed just before the Group 3 JSX is rendered — place it in the desktop return block near the end, immediately before the closing div and Footer. (Group 3 JSX comes in Plan 03, but the formula can be declared now so Plan 03 can reference carX directly.)
    - .planning/phases/02.3-about-us-car-scrollytelling/02.3-PATTERNS.md — Pattern 8 (car animation state and formula) and the src/index.css section describe both additions.
  </read_first>
  <action>
    CHANGE A — src/index.css:
    Add the .car-silhouette rule after the .horizontal-slide.grid-slide-wide block and before any subsequent rules (place it in the non-Tailwind section of the file, alongside other custom component classes). Use the exact rule:
    ```css
    /* F1 car silhouette — absolutely positioned within .horizontal-slide */
    .car-silhouette {
      position: absolute;
      bottom: 20%;
      left: 0;
      width: 50vw;
      max-width: 700px;
      height: auto;
      pointer-events: none;
      will-change: transform;
    }
    ```

    Inside the existing @media (max-width: 767px) block, add the mobile override:
    ```css
    .car-silhouette {
      position: static;
      width: 80vw;
      max-width: 480px;
      will-change: auto;
    }
    ```
    Place the mobile override inside the EXISTING @media (max-width: 767px) block — do not create a new media query block.

    DO NOT add any raw hex values. DO NOT add any new CSS custom properties or tokens. No changes to .horizontal-slide, .horizontal-scroll-group, or any existing rule.

    CHANGE B — src/components/HomepageScrollytelling.tsx:
    In the desktop layout return block (the block starting with `return ( <div ref={containerRef} ...>`), locate the area just before `{/* ── FOOTER */}` (currently near line 751). Add the carX formula as a comment-marked block in the JSX render function scope — place it as a const declaration inside the return block's implicit scope, just above the Group 3 JSX placeholder comment that Plan 03 will fill in:

    ```typescript
    {/* ── Car animation formula (Group 3) ──────────────────────────────── */}
    ```
    Actually, const declarations cannot go inside JSX. Place the formula in the component body BEFORE the mobile `if (isMobile)` check. Add it after all the useEffect hooks and before `if (isMobile) return (...)`:

    ```typescript
    // ── Group 3 car animation formula ───────────────────────────────────────────
    const group3MaxScroll = group3Ref.current
      ? group3Ref.current.scrollWidth - group3Ref.current.clientWidth
      : 1;
    const rawProgress = group3ScrollLeft / Math.max(1, group3MaxScroll);
    const easedProgress = 1 - Math.pow(1 - rawProgress, 1.5);
    const carX = -30 + easedProgress * 36; // vw units: -30vw (off-screen left) → +6vw (resting)
    ```

    This placement means `carX` is available in both the mobile and desktop JSX blocks. Plan 03 will reference `carX` directly in Group 3's panel JSX.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -n "car-silhouette" src/index.css` returns at least 2 matches (base rule + mobile override)
    - `grep -n "position: absolute" src/index.css | grep -c "car-silhouette"` is not required — instead verify: `grep -A 8 "\.car-silhouette {" src/index.css` shows `position: absolute` and `will-change: transform` in the base rule
    - `grep -n "group3MaxScroll" src/components/HomepageScrollytelling.tsx` returns at least 1 match
    - `grep -n "easedProgress" src/components/HomepageScrollytelling.tsx` returns at least 1 match
    - `grep -n "carX" src/components/HomepageScrollytelling.tsx` returns at least 1 match
    - `grep -n "30 + easedProgress" src/components/HomepageScrollytelling.tsx` returns 1 match (formula is present with correct values)
    - `grep -n "will-change: auto" src/index.css` returns at least 1 match (mobile override disables will-change)
  </acceptance_criteria>
  <done>.car-silhouette CSS rule is in src/index.css with absolute positioning, bottom:20%, will-change:transform. Mobile override inside existing media query sets it to static + will-change:auto. carX, rawProgress, easedProgress declared as consts in component body before the mobile branch. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SVG inline in JSX | SVG is authored code — no external data injected into shape attributes |
| group3ScrollLeft as formula input | Integer derived from DOM scrollLeft, bounded by 0..maxScroll — no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.3-02-01 | Information Disclosure | F1CarSilhouette SVG | accept | SVG paths are placeholder geometry — no PII, no proprietary data encoded |
| T-02.3-02-02 | Tampering | carX formula using group3ScrollLeft | accept | scrollLeft is bounded by scrollWidth - clientWidth (browser-enforced); Math.pow output bounded by 0..1 |
| T-02.3-02-03 | Denial of Service | will-change: transform on .car-silhouette | mitigate | Mobile media query sets will-change: auto to prevent GPU layer accumulation on low-memory devices |
</threat_model>

<verification>
- `npm run build` succeeds (tsc + vite build, no output errors)
- F1CarSilhouette renders when placed in a test context (visible as a white/pale silhouette on dark background)
- .car-silhouette positions the car at bottom-20% within a .horizontal-slide parent
- carX starts at -30 (off-screen left) when group3ScrollLeft is 0 and reaches +6 when fully scrolled
</verification>

<success_criteria>
TypeScript compiles. F1CarSilhouette component exists at file scope with correct SVG shapes. .car-silhouette CSS class is in src/index.css with mobile override. carX formula is computed in component body from group3ScrollLeft with ease-out curve.
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-about-us-car-scrollytelling/02.3-02-SUMMARY.md`
</output>
