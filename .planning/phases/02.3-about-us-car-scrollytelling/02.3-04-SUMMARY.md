---
phase: 02.3-about-us-car-scrollytelling
plan: "04"
subsystem: data-pipeline
tags: [shopify, liquid, typescript, data-binding, about-us]
dependency_graph:
  requires: ["02.3-01"]
  provides: ["02.3-05", "02.3-06"]
  affects: ["sections/hero-canvas.liquid", "src/main.tsx", "src/components/HomepageScrollytelling.tsx"]
tech_stack:
  added: []
  patterns: ["Shopify schema settings", "Liquid | json filter", "data-* attributes", "JSON script tag pattern"]
key_files:
  created: []
  modified:
    - sections/hero-canvas.liquid
    - src/main.tsx
    - src/components/HomepageScrollytelling.tsx
decisions:
  - "Used underscore-prefix convention for unused props pending Group 3 render implementation"
  - "Extended HomepageScrollytellingProps interface to unblock TypeScript compile (Rule 3)"
metrics:
  duration: "~10 minutes"
  completed: "2026-06-07"
  tasks_completed: 2
  tasks_total: 2
---

# Phase 02.3 Plan 04: Shopify Data Pipeline for About Us Group 3 Summary

Shopify theme-customizer data pipeline wiring 12 About Us fields via JSON script tag, data-* attributes, and prop pass-through to HomepageScrollytelling.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add schema + JSON script tag + data-* attributes to hero-canvas.liquid | e16de18 | sections/hero-canvas.liquid |
| 2 | Add about content reads and prop pass-through to src/main.tsx | 4cb1b63 | src/main.tsx, src/components/HomepageScrollytelling.tsx |

## What Was Built

- **hero-canvas.liquid**: 12 new schema settings under "About Us — Group 3" header (text, textarea, url types with defaults); `about-content-data` JSON script tag with Liquid `| json` filter for `foundingStory` and `brandParagraph`; 10 new `data-*` attributes on root div with `| escape` filter for XSS mitigation.
- **src/main.tsx**: Reads `about-content-data` script with `JSON.parse` + try/catch + Logger error handling; reads 10 `getAttribute` calls for short strings; passes all 15 props (3 existing + 12 new) to HomepageScrollytelling render call.
- **HomepageScrollytelling.tsx**: Props interface extended with 12 optional props; destructured with underscore prefix convention for `noUnusedLocals` compliance pending Group 3 render implementation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Extended HomepageScrollytellingProps interface**
- **Found during:** Task 2 TypeScript verification
- **Issue:** Plan 01 (which was to add the props interface) ran in Wave 1; the interface was not yet extended at plan execution time, causing `TS2322: Property 'aboutHeading' does not exist on type 'HomepageScrollytellingProps'`
- **Fix:** Added 12 optional props to the interface in HomepageScrollytelling.tsx; used underscore prefix for unused destructured params to satisfy `noUnusedLocals: true` tsconfig constraint
- **Files modified:** src/components/HomepageScrollytelling.tsx
- **Commit:** 4cb1b63

## Known Stubs

The 12 new props are accepted by HomepageScrollytelling but not yet rendered — they are destructured with underscore prefix. The Group 3 UI panels (Plans 02.3-05 and 02.3-06) will consume these props. This is intentional: the data pipeline is wired; rendering is the next wave's responsibility.

## Threat Flags

No new security surface introduced beyond what the threat model already covers. All `| escape` filters applied to data-* attributes; `cta_url` uses Shopify `type="url"` schema; `about-content-data` script tag uses `type="application/json"` (not executed by browser); JSON parse errors caught with Logger fallback.

## Self-Check: PASSED

- sections/hero-canvas.liquid: exists, contains `about-content-data` (1), `data-about-heading` (1), `about_heading` (2), `founding_story` (2), `explore_cta_label` (2), `data-stat-1-value` (1)
- src/main.tsx: contains `aboutHeading` (2 matches), `aboutData.foundingStory` (1), `data-stat-1-value` (1), `exploreCta=` (1)
- Commits e16de18 and 4cb1b63 verified in git log
- `npx tsc --noEmit` exits 0
