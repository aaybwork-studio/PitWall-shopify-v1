---
phase: 02.3-about-us-car-scrollytelling
verified: 2026-06-07T00:00:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 6/8
  gaps_closed:
    - "carX animation is computed from a stable, layout-aware scroll-range value (no stale/zero-division-adjacent reads on first paint or resize)"
    - "Only hand-authored source files are committed for this frontend-only phase (no unreviewable generated bundle checked in as part of the diff)"
  gaps_remaining: []
  regressions: []
deferred: []
human_verification: []
---

# Phase 02.3: About Us Car Scrollytelling Verification Report

**Phase Goal:** Add a Group 3 horizontal-then-vertical scrollytelling section between Group 2 and Footer. The section features a placeholder F1 car SVG animating across 3 horizontal panels driven by scrollLeft, then transitions to a vertical About Us content section. All text is editable via Shopify theme customizer schema.

**Verified:** 2026-06-07
**Status:** passed
**Re-verification:** Yes — after gap closure (commit 6da5086)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Group 3 horizontal scroll section exists between Group 2 and the vertical/footer flow, gated by a dedicated wheel-lock zone | ✓ VERIFIED | Unchanged from prior pass — `group3Ref`, `id="scroll-group-3"`, `isAtG3` zone block, `snapTo(sections[5])` all confirmed present |
| 2 | F1 car silhouette SVG animates across 3 horizontal panels via `translateX` driven by `scrollLeft` | ✓ VERIFIED | Unchanged — `F1CarSilhouette` rendered 3× with `transform: translateX(${carX}vw)`, `carX` derived from `group3ScrollLeft` |
| 3 | `carX` computation is robust to first paint and layout changes (no stale/zero-division reads) | ✓ VERIFIED | **GAP CLOSED (CR-01).** `group3MaxScroll` is now `useState(1)` (line 586), recomputed inside a `useEffect` (lines 587-595) via a `ResizeObserver` attached to `group3Ref.current`; `update()` runs on mount and on every observed resize, eliminating the inline render-time DOM read and adding resize/layout reactivity |
| 4 | Three horizontal panels render the content specified in D-04/D-05/D-06 (About Us wallpaper text, race-track/telemetry separator, founding story + CTA) | ✓ VERIFIED | Unchanged from prior pass — all three panels confirmed present |
| 5 | After Group 3 exits, page transitions into a vertical About Us content section (stats row, brand paragraph, CTA) per D-07/D-08/D-09/D-10 | ✓ VERIFIED | Unchanged — vertical stats/brand/CTA section confirmed wired |
| 6 | Below 768px, Group 3 collapses to natural vertical scroll with a static car image (D-15) | ✓ VERIFIED | Unchanged — mobile early-exit + static `.car-silhouette` override confirmed |
| 7 | All user-facing text (D-16: about_heading, founding_story, cta_label, cta_url, stat_1..3 value/label, brand_paragraph) is editable via Shopify theme customizer schema | ✓ VERIFIED | Unchanged — full schema → data-attr → prop pipeline confirmed traced end-to-end |
| 8 | Only hand-authored source is committed for review; no unreviewable generated bundle in the diff | ✓ VERIFIED | **GAP CLOSED (CR-02).** `.gitattributes` now exists at repo root marking `assets/main.css` and `assets/pitwall-interactive.js` as `linguist-generated diff=generated -merge`, with an explanatory comment that they are vite build outputs from `src/` and must never be hand-edited; README.md already documents them as compiled outputs (lines 12-13, 35-36) |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/components/HomepageScrollytelling.tsx` (Group 3 JSX, F1CarSilhouette, wheel zone, resize-aware scroll-range state) | Group 3 desktop + mobile panels, car animation, scroll-state machine, reactive `group3MaxScroll` | ✓ VERIFIED | All previously-verified pieces intact; `group3MaxScroll` now `useState` + `ResizeObserver`-driven (lines 586-595) |
| `.gitattributes` | Marks generated bundles as `linguist-generated` | ✓ VERIFIED | File exists (304 bytes), correctly lists `assets/main.css` and `assets/pitwall-interactive.js` with `linguist-generated diff=generated -merge` and explanatory comment |
| `sections/hero-canvas.liquid` / `src/main.tsx` | Schema + data pipeline | ✓ VERIFIED | Unchanged from prior pass — confirmed present and wired |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `group3Ref.current` DOM dimensions | `carX` translateX value | `ResizeObserver` → `setGroup3MaxScroll` state → re-render | ✓ WIRED | Replaces the prior fragile inline-read pattern; `update()` called on mount and on every resize observation, keeping `group3MaxScroll` (and therefore `carX`) in sync with actual layout |
| Other links (Group 2→3 snap, wheel→scrollLeft state, schema→props) | — | — | ✓ WIRED | Unchanged from prior pass, re-confirmed no regressions introduced by commit 6da5086 |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/components/HomepageScrollytelling.tsx` | 532-551 | Dead/unreachable "snap-assist" branches | ℹ️ Info (WR-02, pre-existing, non-blocking) | Confusing dead code; does not block goal |
| `src/components/HomepageScrollytelling.tsx` | 365 | Local `const vh` shadows outer `vh` state | ℹ️ Info (WR-03, pre-existing, non-blocking) | Naming confusion only |
| `src/components/HomepageScrollytelling.tsx` | 402/408/435/441/469/476 | `e.deltaY * 0.85` magic number repeated 6× | ℹ️ Info (WR-05, pre-existing, non-blocking) | Tuning constant duplicated |

CR-01 and CR-02 (previously 🛑 Blocker) are now resolved and removed from the anti-patterns table. The three remaining WARNING-level findings are pre-existing code-quality notes that do not block phase goal achievement (as already assessed in the prior verification pass).

### Requirements Coverage

D-01 through D-17: unchanged from prior pass — all 17 decisions remain ✓ SATISFIED with implementation evidence (see prior verification body for full per-requirement detail; no requirement-relevant code changed in commit 6da5086 beyond the CR-01 fix, which strengthens D-02/D-03 implementation robustness without altering their satisfied status).

### Behavioral Spot-Checks

Skipped — scroll-driven UI animation requires a running browser/viewport; cannot be meaningfully spot-checked via shell commands. Plan 06's human-verify checkpoint (19 points, user-approved) stands as the functional-correctness evidence, unaffected by this re-verification's scope (which targeted only CR-01/CR-02 code-level fixes).

### Probe Execution

No probes declared or found — SKIPPED (not a migration/tooling phase).

## Gaps Summary

Both previously-blocking critical gaps are now resolved:

1. **CR-01 (carX fragility) — CLOSED.** `group3MaxScroll` is now tracked as React state (`useState(1)`) and recomputed via a `ResizeObserver` in a `useEffect` attached to `group3Ref.current` (lines 586-595 of `HomepageScrollytelling.tsx`). This eliminates the inline render-time DOM read with magic-number fallback and adds proper resize/layout reactivity — exactly the fix the reviewer recommended in CR-01.

2. **CR-02 (generated bundle undocumented) — CLOSED.** `.gitattributes` now exists, marking `assets/main.css` and `assets/pitwall-interactive.js` as `linguist-generated` build outputs with `diff=generated -merge`, accompanied by an explanatory comment. Combined with README.md's existing documentation of these as compiled vite outputs, the bundle is now properly marked as generated and excluded from review-diff expectations.

No regressions were introduced — the fix is scoped precisely to the two flagged areas, and all 6 previously-verified truths, artifacts, and key links remain intact (re-confirmed by spot reading the surrounding code). The phase goal is fully achieved: Group 3 exists and functions, the car animates robustly across panels with layout-aware scroll-range tracking, the vertical About Us section renders with schema-editable content, mobile collapses correctly, and the source tree is now properly annotated regarding generated assets.

**Score: 8/8 must-haves verified. Status: PASSED. Ready to proceed.**

---

_Verified: 2026-06-07_
_Verifier: Claude (gsd-verifier)_
