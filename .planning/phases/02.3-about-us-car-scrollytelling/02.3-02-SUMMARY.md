---
phase: 02.3-about-us-car-scrollytelling
plan: "02"
subsystem: scrollytelling-car
tags: [svg, css, f1-car, animation-prereq]
dependency_graph:
  requires: []
  provides: [F1CarSilhouette component, .car-silhouette CSS class]
  affects: [HomepageScrollytelling.tsx, src/index.css]
tech_stack:
  added: []
  patterns: [inline SVG JSX, absolute CSS positioning]
key_files:
  created: []
  modified:
    - src/components/HomepageScrollytelling.tsx
    - src/index.css
decisions:
  - "Exported F1CarSilhouette to satisfy noUnusedLocals — Plan 03 will use it at file scope"
metrics:
  duration: "~5 minutes"
  completed: "2026-06-07"
---

# Phase 02.3 Plan 02: F1CarSilhouette Component and CSS Class Summary

**One-liner:** Inline SVG F1 car silhouette component (13 shapes, viewBox 800x200) with absolute CSS positioning class and mobile will-change override.

---

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Add F1CarSilhouette inline SVG component | 93490ea | src/components/HomepageScrollytelling.tsx |
| 2 | Add .car-silhouette CSS class | 0e24f1b | src/index.css |

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing correctness] Exported F1CarSilhouette to satisfy noUnusedLocals**
- **Found during:** Task 1 verification
- **Issue:** `tsconfig.json` has `noUnusedLocals: true`; the component is declared but not yet used in JSX (Plan 03 wires it). TypeScript exits non-zero.
- **Fix:** Added `export` keyword to the function declaration. Plan 03 can import or use it inline — the export adds no overhead and the component remains at file scope.
- **Files modified:** src/components/HomepageScrollytelling.tsx
- **Commit:** 93490ea

---

## Known Stubs

None — this plan delivers infrastructure only (component + CSS). Actual animation wiring is in Plan 03.

---

## Threat Surface Scan

No new network endpoints, auth paths, or trust boundaries introduced. T-02.3-02-03 mitigated: mobile media query sets `will-change: auto` to prevent GPU layer accumulation on low-memory devices.

---

## Self-Check: PASSED

- `grep -n "F1CarSilhouette" src/components/HomepageScrollytelling.tsx` — found
- `grep -n "viewBox.*0 0 800 200" src/components/HomepageScrollytelling.tsx` — found
- `grep -n "car-silhouette" src/index.css` — 2 matches (lines 690, 749)
- `npx tsc --noEmit` — exits 0
